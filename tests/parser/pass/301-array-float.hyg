let n = readInt();
let arr: array {float} = array(n, 40.0f + 2.0f);

let mutable i = 0;
while (i < arrayLength(arr)) do {
    arrayElem(arr, i) <- arrayElem(arr, i) + 1.0f;
    i <- i + 1
};

i <- 0;
while (i < arrayLength(arr)) do {
    assert(arrayElem(arr, i) = 43.0f);
    i <- i + 1
}
