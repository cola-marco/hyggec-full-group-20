let base: int = 10;

let makeOps = fun (offset: int) -> {
    let local: int = base + offset;

    struct {
        add = fun (x: int) -> local + x;
        mul = fun (x: int) -> local * x
    }
};

let ops = makeOps(2);

assert(ops.add(3) = 15);
assert(ops.mul(2) = 24)